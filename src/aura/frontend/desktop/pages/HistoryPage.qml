import QtQuick 2.0
import QtQuick.Controls 2.0

Rectangle {
    color: "transparent"
    Text {
        anchors.centerIn: parent
        text: "History"
        color: Theme.text
        font.pixelSize: 20
    }
}
